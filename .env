# IdeaGPT UI — environment variables
# Copy to .env and set values. No API keys in frontend; backend services use their own keys.

# Base URL of the FastAPI LLM service (required)
VITE_LLM_SERVICE_URL=http://localhost:8000

# Base URL of the RAG lookup service (required).
# For local dev, when the same FastAPI app serves both /llm/* and /api/rag/lookup, set this to the same URL as above.
VITE_RAG_SERVICE_URL=http://localhost:8000

# Optional: React-style names are also read for compatibility (e.g. Create React App)
# REACT_APP_LLM_SERVICE_URL=
# REACT_APP_RAG_SERVICE_URL=
